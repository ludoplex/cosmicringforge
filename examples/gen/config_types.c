/* AUTO-GENERATED by schemagen 1.0.0 — DO NOT EDIT */

#include "config_types.h"
#include <string.h>

void AppConfig_init(AppConfig *obj) {
    memset(obj, 0, sizeof(*obj));
    obj->port = 8080;
    obj->max_connections = 100;
    obj->debug_mode = 0;
    obj->timeout_ms = 5000;
}

bool AppConfig_validate(const AppConfig *obj) {
    if (!obj) return false;
    if (obj->name[0] == '\0') return false;
    if (obj->port < 1 || obj->port > 65535) return false;
    if (obj->max_connections < 1 || obj->max_connections > 10000) return false;
    if (obj->timeout_ms < 0 || obj->timeout_ms > 60000) return false;
    return true;
}

void DatabaseConfig_init(DatabaseConfig *obj) {
    memset(obj, 0, sizeof(*obj));
    obj->port = 5432;
    obj->max_pool_size = 10;
}

bool DatabaseConfig_validate(const DatabaseConfig *obj) {
    if (!obj) return false;
    if (obj->port < 1 || obj->port > 65535) return false;
    if (obj->database[0] == '\0') return false;
    if (obj->max_pool_size < 1 || obj->max_pool_size > 100) return false;
    return true;
}

void ServerConfig_init(ServerConfig *obj) {
    memset(obj, 0, sizeof(*obj));
    obj->log_level = 2;
}

bool ServerConfig_validate(const ServerConfig *obj) {
    if (!obj) return false;
    if (obj->log_level < 0 || obj->log_level > 4) return false;
    return true;
}

