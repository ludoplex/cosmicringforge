/* AUTO-GENERATED by schemagen 2.0.0 — DO NOT EDIT */
#ifndef procmem
#define procmem

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct ProcHandle ProcHandle;
typedef struct MemRegion MemRegion;
typedef struct PatchOp PatchOp;
typedef struct PlatformInfo PlatformInfo;

struct ProcHandle {
    int32_t pid;
    uint64_t handle;
    uint32_t flags;
    int32_t error_code;
    char error_msg[256];
};

struct MemRegion {
    uint64_t base;
    uint64_t size;
    uint32_t protect;
    int32_t type;
    char name[64];
};

struct PatchOp {
    uint32_t id;
    uint64_t address;
    uint64_t size;
    int32_t status;
    uint64_t timestamp;
};

struct PlatformInfo {
    int32_t os;
    int32_t arch;
    uint32_t page_size;
    int32_t can_remote;
    int32_t can_self;
    char backend[32];
};

/* ProcHandle functions */
void ProcHandle_init(ProcHandle *obj);
bool ProcHandle_validate(const ProcHandle *obj);

/* MemRegion functions */
void MemRegion_init(MemRegion *obj);
bool MemRegion_validate(const MemRegion *obj);

/* PatchOp functions */
void PatchOp_init(PatchOp *obj);
bool PatchOp_validate(const PatchOp *obj);

/* PlatformInfo functions */
void PlatformInfo_init(PlatformInfo *obj);
bool PlatformInfo_validate(const PlatformInfo *obj);

#endif /* procmem */
